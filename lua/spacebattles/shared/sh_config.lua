spacebattles.config = spacebattles.config or {}
local config = spacebattles.config


config.CreateNewEnt =  {
    ["pos1"] = 0,
    ["pos2"] = 0
}

config.ExitList = {
    
}

config.debug = {
    
}

config.TotalPortals = 0 -- Change to get number from DB/Saving